# Changelog

## 0.5.0 - 2026-08-19

### Added

- 从当前连接博客汇总已有分类，支持搜索、文章使用次数提示和单击复用。
- 正文工具栏与 `Ctrl/Cmd + Z` 撤回，可分别记录每篇草稿最近的编辑快照。
- 在源码或即时编辑模式的文末按向下方向键可自动补出新行，最多保留两行。

### Changed

- “从博客选择标签”入口升级为可切换面板；再次点击同一入口即可收起，分类与标签选择器会互斥展开。
- 即时编辑器保留文末空行并继续实时回写 Markdown，撤回、保存和发布使用同一正文状态。
- Release 工作流在成功发布后精确清理本项目产生的临时功能与发布分支。

## 0.4.1 - 2026-08-19

### Added

- 连续的即时排版编辑器，可直接使用方向键在任意段落与上下行之间移动和编辑。
- `Ctrl/Cmd + 1…6` 标题、`Ctrl/Cmd + B` 加粗、`Ctrl/Cmd + I` 斜体、链接、列表、引用、行内代码与删除线等常用快捷键。
- 编辑器内置快捷键速查面板，工具栏同时支持 Markdown 源码和即时编辑模式。
- 从已连接博客聚合已有标签，支持搜索、多选、批量添加及标签使用次数提示。

### Changed

- 即时编辑内容通过 GFM 兼容转换层实时回写 Markdown，继续沿用现有的保存、历史版本和发布流程。
- 原有逐块点击编辑改为单一连续文档编辑面，格式变化即时呈现。

## 0.4.0 - 2026-08-19

### Added

- 所见即所得内容块编辑：在预览视图中点击标题、段落、列表或引用即可直接修改对应 Markdown。
- 可复用标签选择器：按当前博客汇总已有标签、使用次数，并支持搜索与批量添加。
- 标签输入支持一次粘贴多个以中英文逗号或换行分隔的标签。

### Changed

- 预览模式升级为可交互的可视化编辑模式，同时保留源码与分栏写作方式。
- Markdown 工具栏可作用于当前正在编辑的可视化内容块。
- 可视化编辑支持 `Esc` 或 `Ctrl/Cmd + Enter` 完成，继续兼容键盘工作流。

### Tests

- 增加博客标签聚合、去重、计数与跨博客隔离测试。
- 服务端渲染测试覆盖所见即所得编辑器及批量标签入口。

## 0.3.0 - 2026-08-13

### Added

- 发布前差异预览，明确展示目标博客、文件路径、公开状态和增删行。
- 基于 SHA-256 的外部修改检测；预览后文件再次变化时会阻止写入并刷新差异。
- 每篇文章最多 20 个本地历史版本，支持自动备份、手动保存、发布前备份和恢复前备份。
- 桌面工作区轮换备份与损坏恢复候选，覆盖博客文件前另存安全副本。
- Frontmatter、差异算法和桌面条件写入的可靠性测试。

### Fixed

- 保留 Mizuki 的加密、别名、永久链接、语言、来源与授权等编辑器未展示的 Frontmatter 字段。
- “保留草稿状态”现在会真实写入 `draft: true`，不再在发布时被强制公开。
- `Ctrl/Cmd + S` 现在立即刷新工作区并创建手动历史版本。

### Changed

- 工作区数据格式升级到 v2，并向后兼容现有草稿。
- 博客同步会标记磁盘冲突，不再静默将外部版本与编辑器副本混合。
- 桌面文件写入改用临时文件、刷盘和原子替换流程。

## 0.2.1 - 2026-08-12

### Added

- 正式支持 Mizuki 的 `src/content/posts` 内容目录。
- 同步时同时识别 `slug/index.md` 与内容目录根部的 `slug.md`。
- 从 Mizuki 同步的单文件文章会继续发布回原文件，新文章采用便于管理素材的 `slug/index.md` 结构。
- 通用 Frontmatter 同时输出 `heroImage` 与 `image`，兼容 Aruma 和 Mizuki 的头图字段。

### Changed

- 博客连接器会自动识别 Aruma 与 Mizuki，无需手动选择博客类型。
- 重写 README，补充下载安装、兼容范围、数据安全和开发发布说明。

## 0.2.0 - 2026-08-11

### Added

- Windows x64 便携版桌面应用，可直接双击运行。
- 多博客连接管理：添加、切换、校验、重连、同步、打开目录和移除。
- 每篇草稿独立绑定目标博客，新草稿默认继承当前博客。
- 桌面工作区持久化，保存草稿、当前文章、主题和博客连接。
- 网页版使用 IndexedDB 保存目录句柄，减少重复授权。
- Electron 桌面启动冒烟测试与 Windows Release 构建命令。
- 推送 `v*` 标签后自动构建并创建 GitHub Release 的工作流。

### Changed

- 发布动作现在始终写入当前草稿绑定的博客。
- 已发布文章的内部标识包含博客连接，避免多博客之间 slug 冲突。

### Safety

- 覆盖已有文章前必须确认。
- 移除连接不会删除任何博客文件。
- 桌面主进程会再次校验 slug 和目标路径，阻止越界写入。
- Markdown 预览会转义原始 HTML，并拦截危险链接协议。
- 桌面文件操作只接受用户通过目录选择器授权过的博客连接。
